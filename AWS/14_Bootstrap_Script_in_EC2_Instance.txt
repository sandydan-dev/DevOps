Bootstrap Script in EC2 Instance

It means run cmd in one place and run application

while creating instance 

> Advanced Settings
> user-data

(write shell script)

#!/bin/bash
sudo su -
sudo apt update -y
sudo apt install nginx -y
echo "Hello Nginx Server" > index.html  (optional)
sudo systemctl start nginx 
sudo systemctl enable nginx 
